{
  colorschemes = {
    rose-pine = {
      enable = true;
      # transparentFloat = true; DEPRECATED (saving for reference in case something doesn't seem right later)
      settings = {
        styles = {
          transparency = true;
        };
        dark_variant = "main";
      };
    };
  };
}
