{
  plugins.notify = {
    # NOTE: was having issues with this plugin and am too lazy to investigate, so disabling for now
    # enable = false;
    enable = true;
    settings = {
      timeout = 1000;
      topDown = false;
    };
  };
}
